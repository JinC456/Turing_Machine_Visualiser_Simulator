import React from 'react';

export default function Canvas({ nodes, edges, setNodes, setEdges }) {
  return (
    <div className="canvas">
      <p>This is the canvas (React Flow)</p>
    </div>
  );
}
